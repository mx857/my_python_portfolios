# Telegram Thread Analyzer

Скрипт анализирует активность в Telegram-группе за **последние 7 дней** и сохраняет результаты в **`result.json`**.

## 🚀 Запуск
Просто выполни:
```bash
python main.py
```

## ⚙️ Что произойдёт
- Скрипт **сам установит все зависимости** (`telethon`, `pytz`).
- При первом запуске он **попросит ввести**:
  - `api_id`
  - `api_hash`
  - ссылку на Telegram-группу (например, `https://t.me/mygroup`)
- Эти данные можно получить с [официального сайта Telegram](https://my.telegram.org).
- После ввода создастся **`config.json`** и начнётся анализ сообщений.

## 📊 Пример результата
```json
{
  "timezone": "Asia/Tashkent",
  "days": [
    {
      "date": "2025-10-15",
      "threads": [
        {"topic": "General Chat", "messages": 64, "users": 21}
      ]
    }
  ]
}
```

Минимум ручных действий — максимум автоматизации ⚡
